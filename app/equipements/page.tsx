import EquipementIndex from '@/components/equipement/equipement-index';

export const metadata = { title: 'Équipements — MaintenancePro' };

export default function EquipementsPage() {
  return <EquipementIndex />;
}
